local cjson = require 'cjson'
local request = require 'lib/request'
local utils = require 'lib/utils'
local redis = require 'lib/redis'
local routing = require 'routing'
local management = require 'manangement' 


describe('Testing Gateway', function() 
  it('Creates an API', function()

  end)
  it('Accepts and forwards requests to that API', function()

  end)
  it('Accepts and forwards requests to that API with headers', function() 

  end) 
  it('Accepts and forwards requests to that API with Query Strings', function() 

  end) 
  it('Accepts and forwards requests to that API with a JSON payload', function() 

  end)
  it('Creates an API with API Key security', function()

  end)
  it('Creates a user to subscribe to that API', function() 


  end) 
  it('Forwards a request to that API with a correct API Key', function() 

  end) 
  it('Rejects requests to that API without a correct API Key', function() 

  end)
end)
